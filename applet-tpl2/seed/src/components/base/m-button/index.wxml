<view 
  hover-stay-time="{{0}}"
  hover-start-time="{{0}}"
  class="btn custom-class {{type}}"
  hover-class="button-hover" 
  style="width: {{width}}; {{customStyle}}"
  bind:tap="onTap"
>
  <view class="loading" wx:if="{{loading}}"></view>
  <view wx:if="{{!loading}}">{{text}}</view>
</view>